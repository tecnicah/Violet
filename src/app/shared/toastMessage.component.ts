export class ToastMessageComponent { /*

    private messages_in:any[] = [];
    private toast_container:any = document.getElementById('toast-container');
    private message_container:any = document.getElementById('toast-text');
    private message_container_title:any = this.message_container.children[0];
    private message_container_message:any = this.message_container.children[1];

    public newToast( message = { title: '', body: '' } ):void {

        if( !this.toast_container.classList.contains('custom-toast--active') ) {

            this.toast_container.classList.add('custom-toast--active');

        }

        this.messages_in.push( message );
        this.showMessagesIn();

    }

    public showMessagesIn():void {

        for( let message = 0; message < this.messages_in.length; message += 1) {

            setTimeout( () => {

                console.log( this.messages_in );
                console.log(this.messages_in[0]);

                this.messages_in.shift();

            }, message * 4200 );

        }

    }
 */
}