import { AssigneeAndFamilyInfoDirective } from './assignee-and-family-info.directive';

describe('AssigneeAndFamilyInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new AssigneeAndFamilyInfoDirective();
    expect(directive).toBeTruthy();
  });
});
